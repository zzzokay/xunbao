# 中国机器人寻宝赛 — 代码审查报告

---

## 一、致命 Bug（必须立即修复）

### 1. 电机输出被注释掉 — 仅右后轮有 PID 控制
**文件：motor_task.c → `handle_pid_control()`**

```c
// motor_set_pwm(1, (int32_t)motor_L0.output);  // 左前轮 ← 注释了！
// motor_set_pwm(2, (int32_t)motor_L1.output);  // 左后轮 ← 注释了！
// motor_set_pwm(3, (int32_t)motor_R0.output);  // 右前轮 ← 注释了！
motor_set_pwm(4, (int32_t)motor_R1.output);     // 仅右后轮在工作
```

**影响：** 在非 `is_Free` 模式下，只有右后轮接收 PID 闭环输出，其他三个轮子可能在吃上一次的 PWM 残留值。这会导致转弯精度低、直线跑偏、爬坡无力等一系列问题。如果小车"正常能跑全程"，可能是因为开环 (`is_Free`) 和陀螺仪补偿在兜底，但性能严重受损。

**修复：** 取消三行注释，恢复四轮 PID 输出。

---

### 2. 赋值写成了比较 — `getZ == 0` 应为 `getZ = 0`
**文件：barrier.c → `South_Pole()` 第1474行**

```c
getZ == 0;   // ← 这是比较表达式，不是赋值！编译器可能不报警
```

**影响：** `getZ` 的值不会被清零，导致南极上坡阶段的陀螺仪角度校准逻辑出错。如果之前一次 `getZ` 被置 1，这次它仍然为 1，后续 `if(getZ == 0)` 分支永远进不去，角度可能取到错误值。

**修复：** 改为 `getZ = 0;`

---

### 3. 除零风险 — `sum_angle / add_time`（至少 4 处）
**文件：barrier.c 多个函数**

```c
// 第998行、1058行、1277行等处：
angle.AngleG = sum_angle / add_time;  // add_time 可能为 0！
```

当传感器没有检测到中间位置（`0x180`），`add_time` 始终为 0，发生浮点除零会产生 `NaN` 或 `Inf`，直接让陀螺仪角度控制崩溃，小车可能原地打转或冲出赛道。

**修复：** 每处除法前加保护：
```c
if (add_time > 0)
    angle.AngleG = sum_angle / add_time;
else
    angle.AngleG = nodesr.nowNode.angle;  // 回退到节点角度
```

---

### 4. 重复条件判断 — 逻辑意图不明
**文件：barrier.c 第1092行、1541行**

```c
if(map.routetime == 0 && map.routetime == 0)  // 两个一模一样的条件
```

**影响：** 第二个条件冗余，可能原意是检查另一个变量（例如 `treasure == 0` 或 `get_a` / `get_b`），若如此则路线规划逻辑存在缺陷。

**修复：** 确认原意，修改为正确的复合条件。

---

## 二、严重问题（影响稳定性和得分）

### 5. 滤波器 `filter()` 完全失效
**文件：filter.c**

`i`, `j`, `temp_angle[8]` 都是**局部变量**，每次函数调用都重新初始化为 0。这意味着：
- 初始化阶段永远只写 `temp_angle[0]`，`i` 从不累积到 8，`first_flag` 可能永远为 0。
- 正常滤波阶段 `j` 每次都从 0 开始，也只写 `temp_angle[0]`，其余 7 个元素是随机栈数据。

**修复：** 将 `i`, `j`, `temp_angle[]` 改为 `static`：
```c
static uint8_t i = 0;
static uint8_t j = 0;
static float temp_angle[8] = {0};
```

---

### 6. `for` 循环初始化缺失
**文件：barrier.c → `Stage()` 第449行**

```c
for(add_time; add_time < 5; add_time++)  // 没有初始化 add_time
```

`add_time` 在前面可能已经被修改过值，这里不重新初始化就开始循环，迭代次数不确定。

**修复：** `for(add_time = 0; add_time < 5; add_time++)`

---

### 7. UART 逐字节阻塞发送
**文件：ArriveDetect_task.c → `send_play_specified_command()`**

```c
for (uint8_t i = 0; i < 7; i++)
    HAL_UART_Transmit(&huart8, &data[i], 1, 0xFFFF);
```

一次发一个字节，超时设为 0xFFFF（约 65 秒），在 FreeRTOS 环境下会阻塞当前任务，影响实时性。

**修复：** 一次性发送整个数组：
```c
HAL_UART_Transmit(&huart8, data, 7, 100);  // 一次发完，合理超时
// 或使用 DMA：HAL_UART_Transmit_DMA(&huart8, data, 7);
```

---

### 8. `delay_us()` 在 FreeRTOS 任务中使用
**文件：gray.c (I2C 软件模拟)**

I2C 软件模拟中大量使用 `delay_us(5)`，这些延时基于 TIM7 轮询，不释放 CPU。在 FreeRTOS 环境下，如果任务优先级高于电机任务，会导致电机控制周期被打断，引起抖动。

**建议：** 若灰度传感器 I2C 通信频率不高，将其挪到低优先级任务，或使用硬件 I2C。

---

## 三、架构与可维护性问题

### 9. 路线表硬编码爆炸 — 超过 3000 行重复代码

`update_rout_by_treasure_7()`, `update_rout_by_treasure_8()`, `get_newroute()`, `update_route_by_door_1~4()` 等函数，本质上就是根据 `color_flag[]` 和 `treasure` 的组合查表，但每个组合都是一个独立的手写数组 + 拷贝循环。

这带来三个问题：
- **极难维护**：改一个节点要改几十处。
- **容易出错**：拷贝粘贴改漏一个节点就会跑错路。
- **浪费 Flash**：大量重复路线占用宝贵存储。

**建议重构方案：**
```c
typedef struct {
    uint8_t color_condition;   // color_flag 组合的编码
    uint8_t treasure_id;       // 宝物编号
    const uint8_t *route;      // 路线指针
    uint8_t route_len;
} RouteEntry;

const RouteEntry route_table[] = {
    { GREEN_D2, 3, route_green_d2_t3, sizeof(route_green_d2_t3) },
    { GREEN_D2, 4, route_green_d2_t4, sizeof(route_green_d2_t4) },
    // ...
};

void apply_route(uint8_t color_cond, uint8_t treasure_val) {
    for (int i = 0; i < ROUTE_TABLE_SIZE; i++) {
        if (route_table[i].color_condition == color_cond &&
            route_table[i].treasure_id == treasure_val) {
            memcpy(&route[map.point], route_table[i].route, route_table[i].route_len);
            return;
        }
    }
}
```

---

### 10. 全局变量泛滥 + 缺乏命名规范

代码中有大量全局变量：`mul2sing`, `sing2mul`, `isStage`, `treasure`, `value`, `DownLiuShui`, `special_arrive`, `get_cude`, `get_a`, `get_b`, `open_qiang_jiao` 等。这些变量分散在多个文件中，没有前缀区分来源，很容易出现命名冲突和竞态条件。

**建议：**
- 按模块加前缀：`barrier_isStage`, `map_treasure`。
- 能用局部变量或结构体封装的就不要用全局变量。
- FreeRTOS 环境下，跨任务共享的变量应使用 `volatile` 或互斥锁保护。

---

### 11. 大量注释掉的代码

整个项目充斥着被注释掉的旧代码（粗略估计 >1500 行）。这些代码不仅增加阅读负担，还容易在调试时误恢复错误版本。

**建议：** 使用 Git 管理版本，删除所有注释掉的旧代码。需要时查历史记录即可。

---

## 四、竞赛性能优化建议

### 12. 速度与 PID 参数自适应

当前的速度和 PID 参数通过 `select_speed()` 按节点速度档位硬编码。建议：

- **弯道前动态减速**：根据 `need2turn()` 的角度大小动态计算减速量，而非固定切到 `Gyro_Speed`。小角度弯道可保持较高速度通过。
- **上下坡 PID 自适应**：根据 `imu.pitch` 实时调整 `kp`/`kd`，上坡时增大驱动力，下坡时增大阻尼。
- **直线加速**：检测到长直线段（`step > 200`）时自动提速，目前只在几个特定节点硬编码提速。

---

### 13. 节点到达检测优化

`deal_arrive()` 使用多个 `if` 逐一判断，每次调用都要遍历所有条件。建议：

- 当前节点的 `flag` 只会匹配一种到达模式，应使用 `else if` 链或 `switch` 替代独立 `if`，提前退出。
- 到达检测任务的 `vTaskDelay(2)` 可以考虑缩短到 1ms 以提高检测精度。

---

### 14. 转弯策略优化

当前转弯分三档：循迹加大kp转弯、陀螺仪差速转弯、原地转。但转弯前的 `Want2Go()` 距离全部是硬编码常数（超过 40 个特殊节点组合），非常脆弱。

**建议：**
- 引入编码器闭环的"前进到节点中心"逻辑，用传感器确认节点位置，而非盲走固定距离。
- 转弯完成的判断可以结合循迹传感器和陀螺仪双重确认，当前只用角度差阈值 2°，有时因振荡过早退出。

---

### 15. 二维码 / OCR 识别容错

`WaitFor_OCR()` 和 `WaitFor_QR()` 的等待逻辑中，若 K210 长时间无响应可能卡死。建议：

- 增加超时保护（例如 3 秒无结果就使用默认值继续）。
- 识别结果做连续 N 帧一致性校验，避免单帧误识别导致路线完全错误。

---

### 16. 流水灯下坡 — 前后轮差速策略可改进

当前下坡时前轮速度乘以固定倍率 `LiuShuiRate`，这个倍率根据障碍类型硬编码。更好的方案是根据 `imu.pitch` 的实时倾斜角动态计算倍率：

```c
float dynamic_rate = 1.0f + (basic_p - imu.pitch) * 0.05f;  // 越陡倍率越大
dynamic_rate = fminf(fmaxf(dynamic_rate, 1.0f), 2.5f);       // 限幅
```

---

### 17. Stage_P2 终点死循环

```c
if(Backtimes == 1)
    while(1);  // 第二次到 P2 直接死循环！
```

如果比赛中意外第二次经过 P2，小车会永远卡死。应改为安全停车或跳过。

---

## 五、优先级排序 — 赛前紧急修复清单

| 优先级 | 问题 | 预计影响 |
|--------|------|----------|
| **P0** | #1 恢复三轮电机 PID 输出 | 直接影响四轮驱动性能 |
| **P0** | #2 `getZ == 0` 赋值修复 | 南极障碍可能跑偏 |
| **P0** | #3 除零保护 | 随机崩溃 |
| **P1** | #5 滤波器 static 修复 | 角度数据噪声大 |
| **P1** | #6 for 循环初始化 | 平台角度计算错误 |
| **P1** | #4 重复条件检查 | 路线规划可能错误 |
| **P1** | #17 P2 死循环 | 比赛中可能卡死 |
| **P2** | #7 UART 优化 | 改善实时性 |
| **P2** | #12-16 性能优化 | 提升速度和稳定性 |

---

> **总结：** 代码的整体架构——地图节点导航 + FreeRTOS 任务调度 + 多模态控制——是合理的。但存在几个致命 Bug（尤其是电机输出被注释、赋值/比较混淆、除零），修复这些后稳定性应有显著提升。路线硬编码虽然不优雅但在赛前改动风险大，建议只修 Bug 不重构架构。性能优化方面，重点放在转弯策略和速度自适应上，这两项对名次影响最大。

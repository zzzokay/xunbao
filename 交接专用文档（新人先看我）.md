# xunbao（寻宝）项目交接手册

> **MCU**: STM32F750V8Tx | **工具链**: MDK-ARM V5.32 | **RTOS**: FreeRTOS + CMSIS_V1 | **主频**: 216MHz
>
> 本文件主要用于交接寻宝机器人项目。
> 推荐使用 Markdown 拓展预览获得最佳阅读效果。
>
> **文档版本**: 2026-08-17 更新。已按当前工作区代码同步过时内容，并补充算法细节与理论基础。

---
前述：这个小车传了好像最长预测有10来年，由于之前交接不够好导致我上手的时候有很多石山代码，我今年有ai比较方便于是开始写交接文档，建议接任者搞完比赛也写一个；并且代码中可能存在魔法数字和神秘逻辑(还有P1和P2反了)，很可能是根据实际情况的硬编码，并不是最佳方案，接任者可以选择从算法优化；并且这个文档很多也是用ai写的，可能存在描述不准的地方；用claude时建议把project_reference和readme喂给它获取上下文；要学会使用ai和git版本管理

## 0. 项目概述

### 0.1 赛题介绍

开始运行到二维码所在平台，获得另外两个存在线索的平台，然后经过指示牌自动规划路线，分别到两个平台，采集完线索合成最终宝物所在平台然后去拿宝藏，最后回家。第二轮根据第一轮获得的线索优化路线。

### 0.2 机器人简介

| 维度 | 说明 |
|------|------|
| **底盘** | 四轮直流编码电机。主要通过循迹板 + 陀螺仪闭环控制走直线或转弯，通过不同状态切换实现丝滑运动 |
| **导航** | 通过引导图导航，更改 `map_message.c` 即可更改小车路线，具有较高的地图适应性 |
| **视觉** | 扫描二维码、数字和颜色（maxicam pro ） |

### 0.3 程序上电运行顺序

#### 为什么这样创建任务？

`main()` 调用 `vTaskStartScheduler()` 后就**不再返回**了（FreeRTOS 接管），所以没法在 main() 里依次做初始化。因此需要一个**引导任务**来完成后台初始化、派生其他工作任务。

> **StartDefaultTask（CMSIS 默认）** → CubeMX 自动生成，不应该改
> **Start_task（用户引导任务）** → 用 `taskENTER_CRITICAL` 保护，防止创建任务时被调度打断，所有工作任务创建完后自删释放栈空间
>
> 套两层是为了**隔离 CubeMX 自动生成的代码**，防止重新生成时被覆盖。

#### 为什么要开临界区？

```c
taskENTER_CRITICAL();     // ← 关调度器中断
main_task_create();       // 创建 main_task
motor_task_create();      // 创建 motor_task
create_ArriveDetect_task();// 创建 ArriveDetect_task
vTaskDelete(NULL);        // 自删
taskEXIT_CRITICAL();      // ← 重新开调度器
```

如果不关中断，刚创建好 `main_task` 但还没创建 `motor_task` 时，调度器可能切过去运行 `main_task`，此时 motor_task 还不存在，一些全局资源也没初始化完，容易出问题。临界区保证 **三个任务一起创建完、一起就绪**，调度器再统一调度。

---

```
上电/复位
  │
  ├─ SystemClock_Config              ← CubeMX 生成，配置 216MHz
  ├─ HAL_Init / MX_GPIO_Init / ...   ← CubeMX 生成，初始化外设
  │
  ├─ MX_FREERTOS_Init()              ← CubeMX 生成，RTOS 初始化
  │   └─ 创建 StartDefaultTask (CMSIS V1 默认入口)
  │
  └─ vTaskStartScheduler()           ← 启动调度器
        │
        └─ StartDefaultTask 运行
             └─ Start_task_create()
                  └─ 创建 Start_task (优先级 6，最高合法优先级)
                  └─ vTaskDelete(NULL)  ← StartDefaultTask 自删
                        │
                        └─ Start_task 运行
                             ├─ user_init()
                             │    ├─ Encoder_init()     ← 编码器初始化
                             │    ├─ Maxicam_Enable()   ← 摄像头初始化
                             │    ├─ imu_receive_init() ← IMU (USART3 DMA)
                             │    ├─ motor_init()       ← 电机 GPIO/TIM 初始化
                             │    └─ vTaskDelay(2000)   ← 等待各模块稳定
                             │         + IMU 上电自检   ← 1s 无数据自动 IMU_Reinit 重试
                             │
                             ├─ taskENTER_CRITICAL()
                             ├─ Rec_usart_init()         ← 接收串口初始化
                             ├─ main_task_create()       ← 创建 main_task    (优先级 5, 栈 2048)
                             ├─ motor_task_create()      ← 创建 motor_task   (优先级 6, 5ms周期)
                             ├─ create_ArriveDetect_task()← 创建ArriveDetect (优先级 5, 栈 512)
                             └─ vTaskDelete(NULL)        ← Start_task 自删
                                    │
                                    └─── 三个任务并行运行 ───┐
                                         │                  │                  │
                                    ┌────┴────┐       ┌────┴────┐       ┌────┴────┐
                                    │main_task│       │motor_task│       │ArriveD..│
                                    │优先级5  │       │优先级6   │       │优先级5  │
                                    │3ms周期  │       │5ms周期   │       │阻塞等待 │
                                    │         │       │          │       │         │
                                    │mapInit()│       │Chassis_  │       │等待     │
                                    │初始化地图│       │Init()    │       │xTask    │
                                    │         │       │电机/PID  │       │Notify   │
                                    │Nav()   │       │初始化     │       │         │
                                    │执行路径  │       │          │       │收到通知 │
                                    │         │       │5ms循环:  │       │→ 检测   │
                                    │         │       │读编码器→ │       │是否到达  │
                                    │         │       │PID→调PWM │       │路口点   │
                                    └─────────┘       └──────────┘       └─────────┘
```

---

## 目录

1. [快速上手 — 15 分钟从零跑起来](#1-快速上手--15-分钟从零跑起来)
2. [调试建议](#2-调试建议)
3. [硬件概览](#3-硬件概览)
4. [软件架构](#4-软件架构)
5. [地图系统详解](#5-地图系统详解)
6. [调参与修改指南](#6-调参与修改指南)
7. [调试模式](#7-调试模式)
8. [已知问题与陷阱](#8-已知问题与陷阱)
9. [算法与理论基础](#9-算法与理论基础)
10. [竞赛实操笔记](#10-竞赛实操笔记)
11. [常见问题 FAQ](#11-常见问题-faq)

---

## 1. 快速上手 — 15 分钟从零跑起来

### 1.1 环境与工具

| 用途 | 推荐工具 | 备注 |
|------|----------|------|
| 编译烧录 | **Keil MDK V5.32** | 必须 V5，V6会编译错误 |
| 写代码 | **VSCode + Claude Code/codex** | 建议安装 ELDE 插件,编译烧录都vscode,还可以学学cmake |
| 下载器驱动 |  ST-Link/DAP(openOCD) 驱动 | 根据硬件型号安装 |

### 1.2 打开工程

1. 打开 `MDK-ARM/test1.uvprojx`
2. 编译，预期 **0 Error 0 Warning**

### 1.3 CubeMX 再生注意事项（⚠️ 重要）

> **每次用 CubeMX 重新生成代码后，必须手动做以下操作：**

1. 注释掉 `main.c` 中的**定时器中断回调函数**（`HAL_TIM_PeriodElapsedCallback`）
2. 注释掉 `stm32f7xx_it.c` 中 `USART3_IRQHandler(void)` 


> 不执行以上操作会报错。

### 1.4 文件编码

所有源文件使用 **UTF-8 编码**。

---

## 2. 调试建议

### 2.1 调试顺序

先调底盘。`Chassis_api` 层已包装好，上层导航（main_task）通过调用 API(实际就是包装好的全局变量) 控制底盘(motor_task)，无需在意底层控制逻辑。
把底盘的每个速度的循迹都跑一跑，转弯的角度测一测，误差要缩小到可接受范围内。
先调内环再调外环，内环反应要迅速，不超调

### 2.2 工具建议

- **串口**: 在运行中调试 PID 等参数非常有用。学会使用环形缓存区。
- **预编译选项**: `main_task.c` 里存在预编译选项，可以直接修改值决定要跑全程还是单个功能调试。

### 2.3 电机调试

如果电机疯转，学会用调试器或串口观察编码器值和输出占空比。根据经验，**经常是编码器坏了**导致 PID 计算出错，使得电机疯转。

- 调试时建议给电机 PWM **限幅**
- 电机占空比**不可以太大**，建议留有 **15% 的余量**，原因有三：① 线会烧 ② 编码器会坏 ③ 驱动板会烧
- 如果小车被障碍卡住，要及时拿起来，要不然堵转也可能导致烧线

如果电机来回疯转，那很可能是5ms周期被破坏，重点考虑是不是printf没删干净，有没有用单精度浮点，有没有优先级调到最高
### 2.4 传感器调试

- **循迹板和圆形激光**: 可以通过拧螺丝调节焦距
- **陀螺仪**: 直接用串口 DMA 空闲中断读，
- **灰度传感器**: 灰度循迹并没有用到，只用于上下坡的陀螺仪角度微调

---

## 3. 硬件概览

### 3.1 主板引脚分配

| 外设 | 引脚 | 功能 |
|------|------|------|
| USART1 | PA9/PA10 | 调试串口（接无线串口模块） |
| USART3 | PB10/PB11 | IMU 陀螺仪（**波特率必须匹配 IMU 模块**） |
| UART4/5/7 | PC10~PD2/PE7/PE8 | 其他串口通信 |
| UART8 | PE0/PE1 |  |
| TIM1/2/3/5 | 编码器输入 | 四路编码器 |
| TIM4/8/9 | PWM 输出 | 四路电机驱动 |
| TIM12 | PB14/PB15 | 舵机 PWM |
| ADC1\2 | PA2/PA3/PC4/PC5 | 电池电压 / 灰度传感器 |

### 3.2 电机与编码器映射

| 电机 | 功能 | 定时器 | 引脚 | 编码器 | 方向逻辑 |
|------|------|--------|------|--------|----------|
| L0 | 左前 | TIM9 | PE5/PE6 | TIM1（正） | 正转: CCRx=0, CCRy=ccr |
| L1 | 左后 | TIM8 | PC8/PC9 | TIM2（正） | 反转: CCRx=ccr, CCRy=0 |
| R0 | 右前 | TIM4 | PD14/PD15 | TIM3（取反） | 同上 |
| R1 | 右后 | TIM4 | PD12/PD13 | TIM5（取反） | 同上 |

> **轮子参数**: 编码器每圈 5720 脉冲，减速比 0.362，轮径 104mm
> **里程计算**: `Distance += (avg_encoder × 10.4 × π / 5720) / 0.362 × LEN_SCALE`（`LEN_SCALE=1.2`，见 9.8）

### 3.3 巡线传感器

- **16路 GPIO 读取**（0 = 黑线，1 = 白底）
- 权重表 `line_weight[16] = {-3...3}`，以车中心对称
- 有效值枚举：`VALID` / `ALL_ERR` / `POS_ERR`
- 详见 `scaner.c`

### 3.4 PWM 参数

- **频率**: 20kHz（TIM 配置: ARR=10800-1, prescaler=0）
- **正常运行**: `MOTOR_PWM_MAX = 8500`（PID 输出上限，`Chassis_Init()` 设置；旧文档的 9800 已过时）
- **转弯限幅**: 5000
- **堵转硬上限**: ≥8000 → 立即刹车死停（`STALL_PWM_ABSOLUTE_MAX`；⚠️ 2026-08-06 起堵转保护已停用，恢复需取消调用点注释）

---

## 4. 软件架构

### 4.1 四层解耦设计（由于历史原因当前解耦还不够规范，有能力的可以学学autosar架构并简单模仿）

```
┌──────────────────────────────────────────────────┐
│                  应用层 (map.c)                    │
│     路线规划、节点调度、障碍触发、多轮竞赛          │
│                                                   │
│   ┌──────────────┐  ┌───────────┐  ┌─────────────┐   │
│   │ Navigation() │ │barrier.c  │  │map_function()│   │
│   │ 路径执行  │  │ 障碍处理   │  │  功能分发    │   │
│   └────┬─────┘  └─────┬─────┘  └──────┬──────┘   │
└────────┼──────────────┼───────────────┼───────────┘
         │              │               │
         ▼              ▼               ▼
┌──────────────────────────────────────────────────┐
│            Chassis API 中间层 (chassis_api.c)      │
│  统一运动控制抽象层 — map.c 不知道底层 PID 细节    │
│                                                   │
│  SetMode() │ SetTargetSpeed() │ SetTrackMode()    │
│  MotorControl() │ Brake() │ DriveDistance()       │
│  EnableAntiSnake() │ EnableWheelieProtection()    │
└────────────────────┬──────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│           电机任务层 (motor_task.c)                │
│             5ms 周期循环，实时控制                  │
│                                                   │
│  读编码器 → 模式切换 → 巡线/转弯/陀螺仪 → PID → PWM │
│  游龙算法 │ 丢线保护 │ 横滚保护 │ 翘头保护          │
└────────────────────┬──────────────────────────────┘
                     │
         ┌───────────┼───────────┐
         ▼           ▼           ▼
┌────────────┐ ┌────────┐ ┌────────────┐
│scaner.c    │ │turn.c  │ │pid.c       │
│16路巡线     │ │转弯控制 │ │增量式PID   │
│巡线检测     │ │原地/陀螺│ │内环速度环   │
│            │ │仪/循迹  │ │外环巡线环   │
└────────────┘ └────────┘ └────────────┘
```

### 4.2 任务与调度

| 任务 | 函数 | 优先级 | 周期 | 职责 |
|------|------|--------|------|------|
| Start_task | `Start_task()` | 6 | 一次性 | 初始化 + 创建三任务后自删 |
| motor_task | `motor_task()` | 6 | 5ms | 编码器 → PID → PWM（核心实时任务） |
| main_task | `main_task()` | 5 | 3ms | `Navigation()` 路径执行（routetime=0/2 时） |
| ArriveDetect_task | `arrive_detect_task()` | 5 | 阻塞等待 | TaskNotify 唤醒后做到达检测 |

> **优先级说明**：FreeRTOS 数值越大优先级越高，`configMAX_PRIORITIES=7`（合法 0~6）。motor_task 与 Start_task 同为最高 6，main_task=5，主任务无法抢占饿死电机任务；ArriveDetect 也是 5。

### 4.3 核心数据流

```
map.c:Navigation() → Chassis_MotorControl(mode, Lspeed, Rspeed, aim)
                → motor_task: handle_xxx_mode()
                    → scaner.c:Go_Line() / turn.c:Go_Angle() / Stage_turn_Angle()
                        → 设置 motor_all.Lspeed / Rspeed
                    → handle_target_speed() → 写 motor_L0.target 等
                    → incremental_PID() → motor_set_pwm()
```

### 4.4 模式枚举与状态转移

```c
typedef enum { is_No=0, is_Free, is_Line, is_Turn, is_Gyro };
```

**巡线子模式**:

```c
TRACK_ALL=0         // 正常双边循迹
TRACK_LEFT_EDGE     // 左单边循迹（忽略右侧）
TRACK_RIGHT_EDGE    // 右单边循迹（忽略左侧）
TRACK_NEAR_CENTER   // 中心就近跟踪
```

`Nav_SegmentInit()` 的默认分支已改为 `TRACK_NEAR_CENTER`，绝大多数普通路段用“中心就近”跑；左右单边用于过门/贴边路段。`L_follow`/`R_follow` 循迹转弯代码保留但当前未启用。

**模式切换时的关键操作**:

| 来源→目标 | 处理 |
|-----------|------|
| Gyro→Line | gyroG_pid → line_pid_obj, TG→TC |
| Line→Gyro | line_pid_obj → gyroG_pid, TC→TG |
| Turn→Line | 清 line_pid / TC / Cspeed |
| Turn→Gyro | 清 gyroG_pid / TG / Gspeed |
| →Turn | 清 gyroT_pid |

---

## 5. 地图系统详解

> **这是整个项目最大的知识块，新人请仔细阅读。**

### 5.1 节点命名规则（非常重要）

代码中定义在 `map.h` 的 `MapNode` 枚举：

| 前缀 | 含义 | 例子 |
|------|------|------|
| **P** | 平台型景点（需爬坡） | P1~P8 |
| **B** | 障碍型景点 | B1~B9（桥、山、波动板等） |
| **S** | 特殊节点（短直立景点） | S1~S5 |
| **C** **N**| 普通路径节点 | C1~C9, N1~N22 | 

**节点编号值**（可在代码中直接用名字引用）：

```
S1=0,  P1=1,  N1=2,  B1=3,  B2=4,  B3=5,  N2=6,  P2=7,  S2=8,  P3=9,
N3=10, N4=11, N5=12, N6=13, P4=14, N7=15, P6=16, B8=17, B9=18, N8=19,
C1=20, C2=21, C3=22, N9=23, N10=24, N12=25, N13=26, P5=27, N14=28,
S3=29, S4=30, N15=31, S5=32, C4=33, C5=34, B4=35, B5=36, B6=37,
B7=38, N16=39, N18=40, N19=41, P7=42, N20=43, N22=44, C6=45, C7=46,
C8=47, C9=48, P8=49, N11=50, G1=51
```

### 5.2 节点数据结构

每个节点定义在 `map_message.c` 的 `Node[126]` 数组中，一行一个连接：

```c
{目标节点, 标志位, 角度, 步长(cm), 速度, 功能}
```

**例** — N5 节点的四行定义：
```c
/*N5 13*/ {N4, RIGHT_LINE|Temp_L|MUL2SING, 0, 84, SPEED3, NONE},
          {N8, CLEFT|DLEFT|DRIGHT, ANGLE_N5N8, DOOR_LEN_N5N8/2, SPEED1, DOOR},
          {N12, DLEFT|LEFT_LINE, 90, DOOR_LEN_N5N12/2, SPEED1, DOOR},
          {N6, MUL2SING|CLEFT|CRIGHT, 180, 150, SPEED2, NONE},
```

| 字段 | 说明 |
|------|------|
| 目标节点 | 从这个节点出发到达的相邻节点 |
| 标志位 | 控制巡线模式、转弯方式、到达检测方式（见 5.3） |
| 角度 | 进入目标节点时的期望方位角（°） |
| 步长 | 本段路径长度（cm），用于里程判断 |
| 速度 | 用 SPEED0~SPEED5 宏（25~75） |
| 功能 | 这个节点上要执行的障碍函数（NONE=普通路口） |

### 5.3 标志位解读

标志位是组合值，控制小车走到这一段时的行为：

| 标志 | 作用域 | 含义 |
|------|--------|------|
| `LEFT_LINE` / `RIGHT_LINE` | 全程 | 单边巡线模式（忽略另一侧白线干扰） |
| `NEAR_CENTER` | 全程 | 中心就近跟踪 |
| `Temp_L` / `Temp_R` | 过半切换 | 走一半后切换到单边巡线 |
| `TEMP_NEAR_CENTER` | 过半切换 | 走一半后切换为中心就近 |
| `L_follow` / `R_follow` | 转弯 | 循迹转弯（沿弧线拐弯） |
| `STOPTURN` | 转弯 | 停车后再原地转 |
| `NOTURN` | 转弯 | 到达后不需要转弯 |
| `MUL2SING` | 到达检测 | 线数从多条变成一条时判到达 |
| `MUL2MUL` | 到达检测 | 多条变多条（十字路口）|
| `DLEFT` / `DRIGHT` | 到达检测 | 左边/右边有线触发 |
| `CLEFT` / `CRIGHT` | 到达检测 | 左斜/右斜线触发 |
| `AWHITE` | 到达检测 | 全白区域触发 |
| `RESTMPUZ` | 动作 | 到达后重置陀螺仪 Z 轴 |
| `SLOWDOWN` | 动作 | 需要减速 |

### 5.4 如何添加新路线(一般不需要)

**如果两个节点之间的连接已经定义在 map_message.c 中**：

1. 在 `map.c` 中声明一个新的 `u8 myNewRoute[100]` 数组
2. 按节点顺序填入节点名，以 `0xFF` 结尾
3. 在 `door()` 或对应逻辑中，根据门颜色结果 `load_route_at()` 加载这条路

**如果需要新增节点**：

1. 在 `map.h` 的 `MapNode` 枚举中追加名称
2. 在 `map_message.c` 的 `Node[]` 数组中添加一行或多行连接
3. 在 `map_message.c` 更新 `ConnectionNum[]`（连接数）和 `Address[]`（起始索引）
4. **所有 `ConnectionNum` 和 `Address` 的索引必须与枚举值严格对应**

### 5.5 路线配置（当前活跃路线）

`map.c` 顶部的 `route[]` 数组控制小车实际走的路径：

```c
// 当前初始路线
u8 route[100] = {B1, N1, P1, N1, B2, N4, N5, 0XFF};
```

全程路径更新点：

1. **P1 读二维码后**：`update_route_at_P1()` 按 `flag_line_clue`（0/3/4）决定去 P3 还是 P4（0=跳过平台直接去门区）
2. **过门后**：`door()` 按 `door_pass[0..4]` 读到的通行状态调用 `update_route_at_door_for_stageAB()` 等，把路线接到东区平台
3. **第二轮开始**：`get_newroute()` 重建“巡游所有平台后回家”的路线（见 5.6）

> 注意：`SKIP_ROUND1=1`（当前工作区）时上电直接预设门状态 + `treasure=6` 进第二轮，方便调车；正式比赛要改回 0。

### 5.6 多轮竞赛逻辑

`map.routetime` 记录当前跑图阶段：`0`=第一轮、`2`=第二轮全程、`3`=全部跑完停车。`mapInit()` 统一做地图状态初始化。

第一轮：初始 `route = {B1,N1,P1,N1,B2,N4,N5,0XFF}`，P1 二维码 → P3/P4 → 门区。`door()` 读 D2~D5/D1 后按门状态接线，前往 P5/P6/P7/P8 采线索，最后按线索回家。

第二轮：`main_task` 检测到 `map.routetime==1` 后调用 `get_newroute()`（其内部 `mapInit()` 会把 routetime 清回 0，所以必须**在调用后**再置 `map.routetime=2`）。`get_newroute()` 先调用 `door_set_pass_node()` 把 8 条门区通行边（N5↔N12、N5↔N8、N3↔N8、N3↔N10）置为 `function=NONE`（二轮过门不重触发 `door()`），再按门状态拼接：

```c
公共段 pre   = {B1,N1,P1,N1,B2,N4,N3,P3,N3,N4,N5,N6,P4,N6,N5}
东区巡游 tour = {N13,P5,N13,N12,N16,N18,B5,N19,C6,B7,N22,C9,P7,C9,N22,B6,N20,P8,
                  N20,C4,C8,C7,N14,C3,N9,B9,N7,P6,N7,B8,N9,N10}
```

- 非 P6 宝藏：进下半区 `entry` 用 `N12`（D2 进）或 `N8→N12`（D3/最外进），最后 `N10` 回程
- 宝藏=P6：进下半区经 `N10` 直达（`entry_D2_p6={N12,N11,N10}`、`entry_D3_p6={N8,N10}`、`entry_far_p6={N4,N3,N8,N10}`），巡游用 `tour_p6`（先深入 P6 再 P8→P7→P5 绕回，终点 N12），回程避开 N11 刀山
- 9 个门状态分支各自拼接 `pre + entry + tour + tail`，`build_round2_route()` 写入 `route[]`

二轮全程 `routetime==2` 使 P7/P8 的 treasure 改路（`map.routetime==0` 门槛）不触发，路线不被截断；跑完 `Nav_TurnAndAdvance()` 使 `routetime→3` 即停，不会无限重启二轮。

---

## 6. 调参与修改指南

### 6.1 参数改哪里

| 想改什么 | 在哪改 | 注意事项 |
|----------|--------|----------|
| 一段路径的速度 | `map_message.c` Node[] 的 speed 字段 | 不同速度对应不同 PID |
| 全局速度等级定义 | `chassis_api.h` 的 SPEED0~SPEED5 | 改了这里所有节点受影响 |
| 巡线灵敏度 | `chassis_api.c` 的 `line_pid_steps[]` 阶梯表 | 按当前实际速度自动选档，见 6.2 |
| 转弯角度 | `map_message.c` Node[] 的 angle 字段 | 用 need2turn() 算最短路径 |
| 转弯前前进距离 | `map.c` `GetForwardDistanceBeforeTurn()` | 目前已用 if 链，建议改为查表 |
| 堵转保护阈值 | `chassis_api.c` `STALL_PWM_ABSOLUTE_MAX` / `STALL_SPEED_RATIO` | ⚠️ 已停用，恢复需取消调用点注释 |
| 翘头保护角度 | `chassis_api.c` `WHEELIE_PITCH_THRESHOLD` | 当前 8.0° |
| 横滚保护角度 | 直接判断 `\|imu.roll - basic_r\| > 40°` | — |
| 丢线保护时间 | `LINE_LOST_THRESHOLD`（5ms × 80 = 0.4s） | — |
| 门区段长度/角度 | `map_message.h` 的 `DOOR_LEN_*` / `ANGLE_*` 宏 | 改一处全局同步 |

### 6.2 各速度对应的外环 PID（根据速度自动切换）

`motor_task` 每 5ms 调用 `Chassis_UpdateLinePidBySpeed()`，按当前**实际编码器速度**从 `line_pid_steps[]` 选档（当前速度 ≤ 档速才采样该档，即尽量向上取高速档低 Kp；超过最高档用最高档）：

| 档速 | Kp | Ki | Kd | 对应速度等级 |
|------|----|----|----|--------------|
| 75 | 3.5 | 0 | 200 | SPEED5 |
| 70 | 3.5 | 0 | 200 | SPEED4 |
| 60 | 4.0 | 0 | 120 | SPEED3 |
| 55 | 5.0 | 0 | 150 | SPEED25 |
| 45 | 6.5 | 0 | 110 | SPEED2 |
| 36 | 7.5 | 0 | 100 | SPEED1 |
| 25 | 9.0 | 0 | 100 | SPEED0 |
| 20 | 11.0 | 0 | 100 | 特低速 |
| 15 | 20.0 | 0 | 140 | 特低速 |
| 12 | 25.0 | 0 | 140 | 特低速 |

**为什么要按实际速度选档**：高速用低 Kp 防摆动，低速用高 Kp 保证跟线。如果减速一开始就切低速高 Kp，车会摇摆；太晚切又不跟线。当前逻辑让 PID 随实际速度逐级下调：减速前期保持高速低 Kp，真正降到档位以下才换更高 Kp。

`line_pid_obj` 用位置式 PID，`outputMax=±80`，`Fspeed` 再乘 `|speed|/40` 解耦转向差速与车速（见第 9 章）。游龙 `Chassis_OverrideLinePid()` 临时覆盖生效时优先，阶梯选择不覆盖它。要强行修改参数，放在速度赋值后、且别与阶梯表打架。

### 6.3 内环速度 PID（通常不动）

| 参数 | L0-L1 | R0-R1 |
|------|-------|-------|
| Kp/Ki/Kd | 40/10/5 | 40/10/5 |
| outputMax | MOTOR_PWM_MAX（巡线/陀螺仪 8500，转弯 5000） | 同左 |

### 6.4 巡线传感器调整

`scaner.c` 中 16 路 GPIO 的**权重表 `line_weight_default[16]`** 决定了巡线修正力度，这个权重可以先按物理长度来，然后看跑的情况加大或减少部分，一直偏向一边就中心加大，大摇摆就可以边缘减小，贴线抖动可以中间减少，其实现在的权重也没有特别严谨，就是看感觉

**寻中线（`calc_near_center`）关键规则**：
- 亮灯总数 ≥ 4 时保守认为线在中心，`error=0` 直行，防旁线/路口干扰拖偏
- 最中心两灯（第 7、8 灯）同时亮 → 直接判中心
- 其余情况扫描**连续亮灯段**（不允许跨空隙取灯），选段中心离场地中心最近的一段，段内**只取最靠中心的 2 个灯**算位置/误差，粗线不会整段平均拖偏

---

## 7. 调试模式

### 7.1 无传感器调试（DEBUG 宏）

`barrier.c:33` 定义 `#define DEBUG 0`（当前默认关闭，走真实传感器；调车时可临时改 1）。

#### 门颜色

`DEBUG=1` 时 `Door_ReadPass()` 从预设数组 `debug_door_pass[5]` 读（2026新规则：黑=不能过/绿=能过/蓝=单相通过）；`DEBUG=0` 时走 `#else` 分支读取真实颜色传感器。

#### 导航线索

```c
DEBUG=1 时，可以直接在barrier里修改
```

非 DEBUG 模式下从摄像头更新这些变量。

### 7.2 单功能测试（MAIN_DEBUG 宏）

在 `main_task.c:27` 定义 `MAIN_DEBUG` 后：

- 程序跳过 `Navigation()` 路径执行
- 执行 `test_flag` / `debug_test_item` 指定的测试项

`test_flag` 测试项（`main_task.c` 顶部）：`1`=循迹测试、`2`=陀螺仪原地 180°、`3`=障碍物测试、`4`=坡道、`5`=红外、`6`=灰度、`7`=十字路口快照、`8`=一键自检、`9`=机器人动作、`10`=摄像头颜色（门）、`11`=OCR 数字、`12`=二维码。




### 7.3 一键自检（Chassis_SelfCheck）

架车黑毯上，调用 `Chassis_SelfCheck()`，每秒监测：

- 陀螺仪漂移 > 1°/s 
- 灰度传感器 AD 值 ≥ 500
- 循迹板 detail ≠ 0 

当前实现只在状态变化时打印，安静时无输出；语音播报代码已注释，需要时可恢复 `send_play_specified_command()`。

### 7.4 串口调试

- 调试串口: USART1 (PA9/PA10)
- `Navigation()` 中会打印 `printf("Current Node: %d\n", nodes.nowNode.nodenum)`
- **⚠️ 不要在 5ms 的 motor_task 循环里加 太多printf**，会破坏周期
- 调试不建议用蓝牙（波特率太慢，会破坏 5ms 周期）

---

## 8. 已知问题与陷阱

### P1 级（可能引发异常）

1. **`map.h` 中 ~120 个 `Clue*route` 外部声明未使用**
   - 全是死代码）
   - 文件末尾的 `ErrorTimes[2]` 和 `TempRoute[50]` 同理
2. **`pid.c` 注释掉的 R1 初始化死代码**
3. **`motor_task.c` 5ms 循环内的调试 printf 块**（当前均已注释；解开后注意会拖慢周期）


### P2 级（可优化但不致命）

4. **`map.c` 两个 `GetForwardDistance*` 转弯前距离判断**
   - 多个 if 条件散落，新加节点时不要模仿这个写法
5. **`filter.c` 4 点去极值滤波窗口过小**（可考虑加大）



### 硬件陷阱

1. **IMU 上电偶发初始化失败（我也不知道为什么）**
   - 兜底修复（2026-08-10）：`Start_task` 上电自检 +1s 无数据自动重启重试

2. **烧电线风险**
   - 千万不要把**负值传入 PWM 的 CCR** 寄存器
   - 要反转电机方向，交换 TIM 两个通道的极性，同时编码器取反

---

## 9. 算法与理论基础

### 9.1 PID 基本概念与项目里的用法

**内环：增量式速度 PID（`pid.c: incremental_PID`）**

电机速度环以 5ms 周期采样编码器速度 `measure`，目标速度 `target`，误差 `bias = target - measure`。增量式 PID 不直接算绝对输出，而是算输出的**增量**：

```text
Δu = Kp × (e_k - e_{k-1})
   + Ki × e_k
   + Kd × (e_k - 2×e_{k-1} + e_{k-2})
u_k = u_{k-1} + Δu
```

- 输出是累加的，天然带积分效果，且不会像位置式那样出现大的输出阶跃，所以控制电机一般用增量
- 项目里加了抗积分饱和：输出已到上限时，只允许积分朝“能退出饱和”的方向积分
- `target==0` 且测量很小时把输出/历史清零，防止电机停止后残留 PWM 抖动

**外环：位置式航向 PID（`pid.c: positional_PID`）**

巡线外环输入是传感器误差 `measure`，输出是左右轮差速 `Fspeed`；陀螺仪外环输入是角度误差，输出也是差速。位置式输出直接等于：

```text
u = Kp×e + Ki×∫e + Kd×差分
```

项目给微分项加了**一阶低通滤波**（`differential_filterK=0.5~0.7`），避免传感器噪声直接放大进差速。

### 9.2 为什么外环输出要按速度缩放

`Go_Line()` 里 `Fspeed *= |speed| / 40`，`Go_Angle()` 里 `GGspeed *= |speed| / 50`。（这里40，50其实没用，主要是乘的那一步）差速转向时角速度近似：

```text
ω ≈ (v_right - v_left) / 轮距
```

如果差速固定而车速提高，转弯半径会变大、路径形状变化。把差速按速度比例缩放后，**同样的航向误差在不同车速下产生近似相同的曲率**，路径形状基本与速度无关，这就是调一个 PID 能覆盖多个速度档的理论基础。

### 9.3 巡线误差为什么用 5 个历史值

`scaner.c` 维护 `line_data[5]` 滑动窗口：

- 每帧算出的误差先过 `coarse_filter`（灯数/线数明显异常直接判无效）
- `pos_detect()` 做位置连续性检查：与最近一次有效位置差 > 1.5 格判为位置跳变
- `Get_scaner_error()` 对 5 个历史值做“多数决 + 簇投票”：正确值多就用正确值平均；没有正确值时，在位置跳变值里找**最密集的簇**（互相接近的值）作为候选（这个算法说实话我不知道它有没有用，地图挺干净，检测的也挺准的）
- 全部无效时返回**上一次有效误差**，而不是 0 —— 丢线瞬间保持最后航向，避免突然直行冲出

理论上是“时间滤波 + 空间聚类”：单帧噪声很难同时骗过位置连续性和 5 帧多数决。

### 9.4 寻中线为什么“只取最中心 2 灯”--一般用于走直线，不希望干扰

`calc_near_center()` 当前规则：

1. 亮灯总数 ≥ 4 → 保守认为路口/旁线干扰，误差强制 0 直行
2. 中心两灯（7、8）同时亮 → 线在正中，误差 0
3. 否则找离场地中心最近的**连续亮灯段**，段内只取最靠中心的 2 个灯算平均位置

如果整段平均，粗线/旁线会把中心点拖偏；如果强制误差 0，偏心细线又不修正。取“连续段内最中心 2 灯”是在两者之间取平衡：线粗时仍追踪中线，干扰时不被拖走。

### 9.5 速度阶梯 PID 的理论动机

同一套 PID 参数无法同时满足高速和低速：高速需要低 Kp/Kd 防摆动，低速需要高 Kp 保证跟线。项目采用“实际速度阶梯”而非“目标速度阶梯”，是因为减速过程有惯性：如果刚发减速指令就切低速高 Kp，车还很快时会振荡；只有实际速度真正降下来再切，才是稳定切换。

### 9.6 游龙防抖（AntiSnake）

游龙是巡线摆动/蛇行的保护：

- `|Scaner.error| > 1.0` 时累计 `anti_snake_err_count`，回正后快速衰减
- 达到阈值后把 `motor_all.Cspeed` 降到目标速度的 1/3（最低 15），必要时 `OverrideLinePid(30,0,180,30)` 强压 PID
- 恢复后回到 `target_speed` 并恢复 PID

本质是“检测到持续横向误差 → 主动降速提高阻尼 → 恢复”。

### 9.7 转弯算法的细节

- `need2turn(now, target)` 把角度差归一到 `(-180, 180]`，保证走最短旋转路径
- `Turn_Angle_Base()`：误差 < 2° 判到位；输出太小但误差仍存在时给固定 ±7 死区补偿，避免低速摩擦导致停在中间
- `Stage_turn_Angle()` 传 `force_threshold=150`：偏离超过 150° 时强制向右转（只是为了每次转的方向一样）
- `Turn360Step()` 用梯形速度曲线：加速 40° → 全速 → 358° 前减速，比固定 PID 脉冲更平稳、末端不冲

### 9.8 里程与长度标定

```text
Distance += (encoder_avg × 10.4 × π / 5720) / 0.362 × LEN_SCALE
```

编码器每圈 5720 脉冲、减速比 0.362、轮径 104mm；`LEN_SCALE=1.2` 是实测标定（100 代码单位 ≈ 120cm）。地图 step 和所有距离阈值已经按厘米内联折算，只有运行时连续累加的里程公式乘 `LEN_SCALE`。

### 9.9 IMU 与角度补偿

`basic_y/basic_p/basic_r` 是上电零位校准（10 次采样平均），`imu.compensateZ` 由 `mpuZreset()` 算出，使 `getAngleZ() = yaw + compensateZ` 归一到 `[-180,180]` 并等于参考角。转弯、陀螺仪直行、角度判断都基于 `getAngleZ()`，所以上电后必须等 IMU 数据稳定再做 `IMU_Calibrate_Yaw()`。

### 9.10 FreeRTOS 实时性与性能

- motor_task 是最高优先级 6，用 `vTaskDelayUntil` 固定 5ms 周期；main_task 优先级 5 不会抢占饿死它
- 5ms 循环里不要做阻塞操作或大量 `printf`（打印会拖慢周期）
- Cortex-M7 的 DWT 周期计数器默认锁写，调试测 CPU 耗时要先 `DWT->LAR = 0xC5ACCE55`
- M7 只有单精度 FPU：浮点常量写 `f` 后缀，用 `fabsf/sinf/strtof`，避免软件 double 拖慢中断和 PID

---

## 10. 竞赛实操笔记

### 10.1 常见调参流程

```
车跑偏 → 检查 line_weight_default[] 是否对称
        → 检查 line_pid_steps[] 当前档位的 kp/kd（按实际速度取档）
        → 检查机械结构 / 四个轮子 PWM 一致性

转弯不准 →重心是有偏的，离心力导致车偏，可以减少转速 ，或者通过硬补偿，修改 Turn_Angle_Base 的 ratio 参数（不一定有用）
         → 检查 need2turn() 的角度计算
         → IMU 归零是否准确

过障碍不稳
         优先优化算法，不行再硬补偿
         → 降低对应段的速度
          → 检查障碍状态机逻辑
          → 确认灰度传感器读数正常
```

### 10.2 二轮路线与门区调试

- 想快速测二轮：`map.h` 设 `SKIP_ROUND1=1`，门状态在 `main_task.c` 初始化处预设（索引 0:D2、1:D3、2:D4、3:D5、4:D1），`treasure` 预设宝物平台
- 二轮路线由 `get_newroute()` 拼接，改门区长度/角度只改 `map_message.h` 的 `DOOR_LEN_*` / `ANGLE_*` 宏
- 现场只调某一段：先查该段在 `map_message.c` 的 `Node[]` 行（step/speed/flag），再决定改距离还是改 PID 档位
- 改完路线用 `map_message.c` 连接表逐边核对连通性，防止 `getNextConnectNode()` 找不到连接返回 0

---

## 11. 常见问题 FAQ

**Q: 小车不走直线？**
A: 检查顺序：`line_weight_default` 权重表 → `line_pid_steps[]` 当前档 kp/kd（注意按实际速度取档）→ 四个轮子 PWM 一致 → 机械结构

**Q: 电机疯转怎么办？**
A: 串口观察编码器值是否异常。大概率是编码器坏了导致 PID 计算出错。先限幅再排查。

**Q: 不同场地需要改什么？**
A: 主要是map里的距离补偿。

**Q: 二轮过门被误判/直冲某节点？**
A: 先看 `door()` 的状态判定（按 lastNode/nowNode 精确匹配）和 `get_newroute()` 预设的 `door_pass[]`，再查门区段 `DOOR_LEN_*`/`ANGLE_*` 宏。改完路线务必按连接表逐边核对。

---

## 附录

- [详细技术参考文档 → project_reference.md](project_reference.md)（所有 API、数据结构、常量、Bug 清单）
- [README → 修改记录](README.md)（按日期追溯所有改动）
- 代码修改请同步更新 `README.md` 和 `project_reference.md`

---

> **最后提醒**: 竞赛机器人是"代码 + 机械 + 场地 + 运气"的综合体。
> 很多问题不是代码能解决的，学会区分"代码 bug"和"需要调参/调机械/排除电路问题"是上手最快的方式。

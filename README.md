# xunbao
4.14（张东骏）
加入command.c作为调试串口缓存区
发现：每次mx生成完都要注释掉main的定时器中断回调，和stm32f7xx.....里的USART3_IRQHandler(void)
删掉了stm32....里的void UART5_IRQHandler(void)的标志位判定，改用hal自带的api，HAL_UARTEx_ReceiveToIdle_IT
加入串口1，4，7
测试：串口5发送正常，但无法进入中断handler;     使用串口7甚至无法发送？
    串口4发送成功；    无法进入中断，仍然接收不成功？    改用HAL_UART_Receive_IT；   依然接收不成功？疑似杜邦线问题？（发现：中断里没重新调用）
    蜂鸣档测完，杜邦线正常
    成功了：改变了串口，改变了api；
    oka发现了就是串口问题，现在改回 HAL_UART_Receive_IT(&huart4,Rx_data,BUFFER_SIZE_rec) 可以正常收发
    再试一次串口7。。。。坏了


4.18（张东骏）
测试，新板子串口4，5，7均能正常发送，选择串口4
问题：串口发送接收正常，能成功改变参数，目标值无法改变，且在0附近抖动，疑似与转向环有关（解决：注释掉转向环，现在直接改变lspeed和Rspeed）
问题：突然间速度很快很快，咋回事？（解决：方向反了）
修改：goline(),runwithangle,将motorall最为参数传入，而不是全局变量，
修改：在scanner.c里增加注释
修改：对轮子测量值进行窗口滤波处理 效果：output变稳定了，应该能提高稳定性，选择保留

可选择：目前pid参数还行，备选（20；2；0）

发现：R1的pid参数为零？修改：补全；

教训：烧了电线，原因是太鲁莽，把负的数传入ccr，想更改电机方向只需修改tim两个通道顺序，同时编码器方向取反

重点问题（待解决）：四个轮子测试单独测试时都能正常运行，但是一起运行时左边正常，右边发疯（应该是正反馈）
    现象：右边编码器方向derction为-1时，右前轮向后疯转，右后轮向前疯转；
    保护修改：在pid内限幅1000
    现象：代码中选R0原注释是右前轮，但实际转的是右后轮（待修改）
    现象：尝试将R0的两个通道交换顺序，并且编码器取反，结果右边两个轮子开始鬼畜？（还好加了保护，要不然又烧了）
    现象：单独右边轮子的时候感觉似乎偏快
    现象：四个轮子都给500ccr的时候都正常向前转
    猜测：是否存在编码器与电机错位

使用提示:要调试当个轮子时只需修改rec_uart.h里的宏定义和motortask里的set_pwm;
        
